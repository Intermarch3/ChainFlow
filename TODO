TODO:
- Add the transaction to delete and retrieve funds from the Chainlink contract.  
- Perform a real simulation of the LINK required for the subscription.  
- Improve security with a proxy.  


Steps to Cancel a subscription:
- Call `cancelSubscription` in the ChainFlow contract.  
- Call `cancelUpkeep(uint256 id)` and `withdrawFunds(uint256 id, address to)` in the Chainlink registry contract.  
